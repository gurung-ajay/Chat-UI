/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "plus.unsplash.com",
            "thumbs.wbm.im",
            "images.unsplash.com",
            "www.machovibes.com"
        ]
    }
};

export default nextConfig;
